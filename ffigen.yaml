# Run with `flutter pub run ffigen --config ffigen.yaml`.
name: SystemProxyResolverBindings
description: |
  Bindings for `src/system_proxy_resolver.h`.

  Regenerate bindings with `flutter pub run ffigen --config ffigen.yaml`.
output: 'lib/src/system_proxy_resolver_bindings_generated.dart'
headers:
  entry-points:
    - 'src/system_proxy_resolver.h'
  include-directives:
    - 'src/system_proxy_resolver.h'
compiler-opts:
  - '-Ithird_party/dart-sdk/src/runtime/include/'
preamble: |
  // ignore_for_file: type=lint
comments:
  style: any
  length: full
